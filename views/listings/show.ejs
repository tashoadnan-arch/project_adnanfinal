
<% layout("/layouts/boilerplate")%>
<!-- <script>
  const mapToken="<= process.env.MAP_TOKEN %>";
  // const coordinates ="<- JSON.stringify(listing.geometry.coordinates) %>" ;
   const listing =<- JSON.stringify(listing) %> ;
</script> -->
<script>
  const mapToken = "<%= process.env.MAP_TOKEN %>";
  const listing = <%- JSON.stringify(listing) %>;
</script>



 <!-- to check the whole go styling review on part a of proj -->
    <body>
          <div class="col-9 offset-3 mt-2">
          <h3><b><%=listing.title%></b></h3>
         </div>
     <div class="col-6 offset-3 show-card listing-card">
     <div class="card listing-card">
     <img src="<%= listing.image.url %>" class="card-img-top show-img" alt="listing_image"/>
     <div class="card-body">
        <p class="card-text">owned by   <i><%= listing.owner.username%> </i></p>
          
            <!-- <b><=listing.title%</b><br>    ye  listing.owner.username 57 project ka part hai  -->
           <p class="card-text">  <%=listing.description%> </p><br>   
           <p class="card-text">  &#8377;<%=listing.price%></p><br> <!--.toLocaleString("en-IN")>-->
          <p class="card-text">   <%=listing.location%> </p><br>
            <p class="card-text"> <%=listing.country%></p><br>
        </p>
     </div>
     </div>
                <!-- 57 proj  there was if condtion for listing not required-->
        <%if(currUser && listing.owner._id.equals(currUser._id)){%>
    <div class="btns ">
    <a href="/listings/<%=listing._id%>/edit" 
        class="btn btn-dark col-1 offset-1 " 
        style="height: 2.4rem;">edit  </a>
        <br><br>
    <form method="post" action="/listings/<%=listing._id%>?_method=delete"> 
    <button class=" btn btn-dark offset-3">delete</button>
    </form>
    </div>
 <%}%> 
           

      <div class="col-12 offset-0">
        <%if(currUser) {%>
             <hr>
        <h4>leave a review</h4>
        <form action="/listings/<%=listing.id%>/reviews" method="post" novalidate class="needs-validation">
            
          <!-- <div class="mb-2 mt-3" >
            <label for="rating" class="form-label">Rating</label>

            <input type="range" min="1" max="5" id="rating" name="review[rating]" class="form-range">
            </div>-->

             <div class="mb-1 mt-1" > 
                   <label for="rating" class="form-label">Rating</label>
                    <fieldset class="starability-slot">
                    
 
  <input type="radio" id="no-rate" class="input-no-rate" name="review[rating]" value="1" checked aria-label="No rating." />
  <input type="radio" id="first-rate1" name="review[rating]"  value="1"/>
  <label for="first-rate1" title="Terrible">1 star</label>
  <input type="radio" id="first-rate2" name="review[rating]" value="2" />
  <label for="first-rate2" title="Not good">2 stars</label>
  <input type="radio" id="first-rate3" name="review[rating]" value="3" />
  <label for="first-rate3" title="Average">3 stars</label>
  <input type="radio" id="first-rate4" name="review[rating]" value="4" />
  <label for="first-rate4" title="Very good">4 stars</label>
  <input type="radio" id="first-rate5" name="review[rating]" value="5" />
  <label for="first-rate5" title="Amazing">5 stars</label>
</fieldset>
      </div>
            <div class="mt-2 mb-2">
                <label for="comment"  class="form-label">Comment</label>
                <textarea  id="comment" cols="10" name="review[comment]" class="form-control" required></textarea>
                <div class="invalid-feedback">
                    please add some commment
                </div>
            </div>
            <button class=" btn btn-outline-dark mb-5">submit</button>
        </form>

        <%}%>
           
            <!-- <h4>all Reviews</h4> -->
           
            <!-- <p><=listing.reviews%></p>  in 7th of part a--> 
             <!-- <ul>
                <for(review of listing.reviews){%>
                        <li> <=review.comment%> ,<review.rating%> stars</li>
                <}>
             </ul> -->
             <% if(listing.reviews.length > 0){%>
           <div class="row">
      <% for (let review of listing.reviews) { %>
              <p><b>All Reviews</b></p>
              <div class="card col-5 ms-3 mb-3">
                <div class="card-body">
             <h5 class="card-title">@<%= review.author.username %></h5>
              <p class="starability-result card-text" data-rating="<%= review.rating %>">
            <span class="visually-hidden">Rated: <%= review.rating %> stars</span>
              </p>

          <p class="card-text"><%= review.comment %></p>
        <!-- <p class="card-text"><= review.rating %> stars</p> -->

         <form class="mb-1" method="POST"
              action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE">
          <button class="btn btn-sm btn-dark">Delete</button>
        </form>
      </div>
    </div>

  <% } %>
      </div>
      <%}%>
  </div><hr>
             <div class="col-8 offset-0 mb-0 ">
              <h3>where you will be</h3>
               <div id="map" ></div>
                        
  
             </div>

           
</div>
<script src="/js/map.js"></script>


 
</body>
      
        
    
   

